import { StyleSheet } from 'react-native';

export const globlaStyle = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    justifyContent: 'center',
  },
  input: {
    borderColor: '#000',
    borderWidth: 1,
  }
});